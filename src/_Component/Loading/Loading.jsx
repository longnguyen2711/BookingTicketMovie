import React, { Fragment, useState } from "react";

export default function Loading(props) {
  const { isLoading } = useState((state) => state.LoadingReducer);

  return (
    <Fragment>

    </Fragment>
  );
}
